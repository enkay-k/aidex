name: AIDE X CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  issues:
    types: [opened, closed]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run Tests
        run: |
          echo "Running AIDE X tests..."
          # TODO: Add test commands
      
      - name: Lint
        run: |
          echo "Running linters..."
          # TODO: Add lint commands

  notify:
    needs: test
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Discord Notification
        uses: ntbot/discord-webhook-action@main
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status: ${{ job.status }}
